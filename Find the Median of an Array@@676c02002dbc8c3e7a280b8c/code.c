#include <stdio.h>

int main() 
{
    int n, sum;
    scanf("%d", &n);  // Input the size of the array
    int arr[n];
    
    // Input the elements of the array
    for (int i = 0; i < n; i++) 
    {
        scanf("%d", &arr[i]);
    }
}